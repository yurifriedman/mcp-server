# Gmail MCP Server Configuration

# Path to your OAuth 2.0 credentials JSON file
GMAIL_CREDENTIALS_PATH=./client_secret_*.json

# Path where the OAuth token will be stored after authentication
GMAIL_TOKEN_PATH=./token.json

# Default maximum results per query (1-500)
GMAIL_MAX_RESULTS=50

# Rate limit delay between API calls in milliseconds
GMAIL_RATE_LIMIT_DELAY=100
